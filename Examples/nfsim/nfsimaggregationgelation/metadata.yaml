id: nfsim_aggregation_gelation
name: nfsim aggregation gelation
description: A model for simulating aggregation and gelation processes in nfsim

authors:
  - name: string
    orcid: string

contributors:
  - name: Achyudhan

citation:
  doi: string
  pmid: string
  url: string
  reference: string

date:
  created: YYYY-MM-DD
  modified: YYYY-MM-DD
  published: YYYY-MM-DD
  last_tested: 6/24/2026

tags: string[]

biol_category:
enum(signaling, regulation, metabolism, gene-expression, epidemiology, immunology, synthetic-biology, ecology, physics, other, biology, genetics, signal-processing, mechanistic-modeling, biophysics)

comp_categories:
enum(tutorial, validation, showcase, computer-science, other, compartments, energy, feature-demos, nfsim, processes, wacky, cs, ml, other)

compatibility:
  rr_compatible: True
  bnglviz_compatible: True
  bng2_compatible: True
  vcell_compatible: True
  vcell_errors: False
  min_bng_version: string
  simulation_methods:
    - ode
    - ssa
    - nf 
    - molclustpy
    - vcell
  nfsim_compatible: True
  molclustpy_compatible: True
  excluded: boolean

Features:
  uses_generate_network: 
  default_sim_command:
  enum(ode, ssa, nf, pla, hybrid,molclustpy,vcell)
  uses_cbngl_compartmts: boolean
  uses_vcell_compartmts: boolean
  uses_energy: boolean
  uses_functions: boolean
  uses_totalrate: boolean
  uses_moveconnected: boolean
  uses_trash_molecules: boolean
  uses_anchors: boolean
  uses_multiple_identical_sites: boolean
  uses_deletes_molecules: boolean
  uses_exclude_include_reactants: boolean

source:
  origin: enum(published, contributed, ai-generated, ported-from-sbml, tutorial, test-case)
  original_format: string
  original_repository: string
  biomodels_id: string
  source_path: string
  aux_dirs: string[]

collection:
  type: enum(parameter-fit-variants, sensitivity-analysis, geographic-variants, patient-specific, benchmark-suite)
  parent_model: string
  variant_key: string
  count: integer

playground:
  visible: boolean
  gallery_categories: string[]
  featured: boolean
  difficulty: enum(beginner, intermediate, advanced)
  sort_priority: integer